---
phase: 01-build-static-site-content-portraits-palette
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hugo.toml
  - .gitignore
  - themes/arcaeon/
  - static/images/portraits/matt.jpg
  - static/images/portraits/jaye-anne.jpg
  - static/fonts/
  - assets/css/rewired-overrides.css
  - content/_index.md
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-04
  - HERO-03
  - HERO-04
user_setup: []

must_haves:
  truths:
    - "Running `hugo version` succeeds and `hugo` builds the site without errors"
    - "The arcaeon theme is vendored at `themes/arcaeon/` and Hugo resolves it as the active theme"
    - "ARCÆON palette tokens are available as CSS custom properties on `:root`"
    - "Both host portraits exist at `static/images/portraits/matt.jpg` and `static/images/portraits/jaye-anne.jpg` as 1:1 square JPEGs"
    - "Swapping Jaye Anne's portrait is a single-file replacement — no code edit required"
    - "Serif display (Fraunces or inherited equivalent) + clean sans body (Inter or inherited equivalent) render on headings and body respectively"
  artifacts:
    - path: "hugo.toml"
      provides: "Site config: baseURL, title, theme = arcaeon, ARCÆON palette params"
      contains: 'theme = "arcaeon"'
    - path: "themes/arcaeon/layouts/_default/baseof.html"
      provides: "Base HTML shell copied from falkensmage-website theme"
    - path: "themes/arcaeon/assets/css/main.css"
      provides: "ARCÆON palette tokens + triad layout rules"
    - path: "static/images/portraits/matt.jpg"
      provides: "Matt's locked Feral Architecture portrait"
    - path: "static/images/portraits/jaye-anne.jpg"
      provides: "Jaye Anne's tentative v3 portrait (swap-ready single asset)"
    - path: "assets/css/rewired-overrides.css"
      provides: "Rewired-specific CSS: typography override (if theme fonts insufficient per D-04), warm-core palette usage tier, responsive breakpoints per D-12"
    - path: "content/_index.md"
      provides: "Homepage content stub with empty frontmatter — copy lands in Plan 02"
  key_links:
    - from: "hugo.toml"
      to: "themes/arcaeon/"
      via: "theme = \"arcaeon\" setting resolves to themes/arcaeon/ directory"
      pattern: 'theme\s*=\s*"arcaeon"'
    - from: "themes/arcaeon/assets/css/main.css"
      to: ":root CSS variables"
      via: "--arcaeon-void-purple, --arcaeon-fusion-gold, etc. defined at :root"
      pattern: "--arcaeon-void-purple:\\s*#1a0f2e"
    - from: "static/images/portraits/*.jpg"
      to: "Hugo static pipeline"
      via: "Served at /images/portraits/matt.jpg and /images/portraits/jaye-anne.jpg at runtime"
      pattern: "static/images/portraits/matt\\.jpg"
---

<objective>
Scaffold the Rewired Hugo project, vendor the arcaeon theme from falkensmage-website, wire ARCÆON palette tokens, install host portraits as swap-ready static assets, and establish the typography foundation.

Purpose: Every downstream plan depends on a working `hugo build` that resolves the arcaeon theme, has palette tokens available as CSS variables, and has the two portrait assets on disk. This plan produces that foundation.

Output: A buildable Hugo project that renders (even if the homepage is still a stub) with palette, fonts, and portraits all in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-build-static-site-content-portraits-palette/01-CONTEXT.md

Reference project (Hugo + arcaeon theme — WORKING EXAMPLE):
- `/Users/falkensmage/RitualSync/falkensmage-website/hugo.toml` — canonical theme wiring + palette params
- `/Users/falkensmage/RitualSync/falkensmage-website/themes/arcaeon/` — theme source (layouts, assets/css, static/fonts)

Palette canonical reference:
- `~/.psyche/swipe-files/podcasting-creative/2026-04-16-12-33-18-arc-on-color-palette-full.md`

Portrait source files:
- `~/Documents/Business-Brand/Feral-Architecture/portraits/2026-04-13-author-headshot/final/author-headshot.jpg` → becomes `static/images/portraits/matt.jpg`
- `~/Documents/Business-Brand/Feral-Architecture/portraits/2026-04-18-jaye-anne-headshot/final/jaye-anne-headshot.jpg` → becomes `static/images/portraits/jaye-anne.jpg`

<interfaces>
<!-- Palette tokens defined in themes/arcaeon/assets/css/main.css :root — use these names in Plan 02 -->
<!-- Extracted from falkensmage-website/themes/arcaeon/assets/css/main.css -->

CSS custom properties available at :root after theme is vendored:

```css
/* Primary Identity */
--arcaeon-electric-violet: #7a2cff;
--arcaeon-neon-magenta:    #ff3cac;
--arcaeon-plasma-pink:     #ff5fd2;
--arcaeon-electric-blue:   #2fd3ff;

/* Focal Energy (Radiant Core — Rewired's primary register) */
--arcaeon-solar-white:     #fff4e6;
--arcaeon-fusion-gold:     #ffb347;
--arcaeon-ignition-orange: #ff7a18;

/* Cosmic Depth */
--arcaeon-deep-indigo:     #0a0f3c;
--arcaeon-midnight-blue:   #111a6b;
--arcaeon-void-purple:     #1a0f2e;

/* Energy Accent */
--arcaeon-ion-glow:        #5be7ff;

/* Semantic aliases the theme already provides */
--color-bg:         var(--arcaeon-void-purple);
--color-bg-alt:     var(--arcaeon-deep-indigo);
--color-text:       var(--arcaeon-solar-white);
--color-accent-warm: var(--arcaeon-fusion-gold);
--color-cta-start:  var(--arcaeon-fusion-gold);
--color-cta-end:    var(--arcaeon-ignition-orange);
```

Section layout classes the theme ships:
- `.section-void` — Void Purple background, gold/orange accents (Rewired homepage should use this)
- `.section-depth` — Deep Indigo background (alternative for section alternation)

Fonts the theme currently ships (Cinzel + Space Grotesk) live at:
- `themes/arcaeon/static/fonts/cinzel-latin-wght-normal.woff2`
- `themes/arcaeon/static/fonts/space-grotesk-latin-wght-normal.woff2`

Cinzel is a literary serif (reads as adult/literary — satisfies D-04's intent). Space Grotesk is a geometric sans (satisfies D-04's body requirement). **Decision to make in Task 3:** if executor judges Cinzel + Space Grotesk as acceptable against D-04's "Fraunces/Inter or a close equivalent" clause, INHERIT them and skip the font swap. If executor judges them insufficient, override in `assets/css/rewired-overrides.css` using Fraunces (Google Fonts) + Inter (Google Fonts) with `@font-face` declarations served from `static/fonts/`. Document the decision in the task's `<done>` output.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Hugo project and vendor the arcaeon theme</name>
  <files>hugo.toml, .gitignore, themes/arcaeon/ (entire tree copied from falkensmage-website), content/_index.md</files>
  <read_first>
    - /Users/falkensmage/RitualSync/rewired-site/.planning/phases/01-build-static-site-content-portraits-palette/01-CONTEXT.md (D-01, D-11)
    - /Users/falkensmage/RitualSync/falkensmage-website/hugo.toml (entire file — this is the wiring template)
    - List /Users/falkensmage/RitualSync/falkensmage-website/themes/arcaeon/ to understand the tree being copied
  </read_first>
  <action>
    Per D-01: scaffold a Hugo project that consumes the arcaeon theme. The source theme is NOT a standalone git repo (verified — no `.git` inside `themes/arcaeon/`, no `.gitmodules` in falkensmage-website), so Hugo module and git submodule paths are both unavailable. Fall back to **vendoring the theme by direct copy** into `themes/arcaeon/`. Document this deviation in the Plan 01 SUMMARY.

    Steps:
    1. `cd /Users/falkensmage/RitualSync/rewired-site` (confirm you are in the rewired-site project root, NOT psyche-ws).
    2. Copy the theme tree: `cp -R /Users/falkensmage/RitualSync/falkensmage-website/themes/arcaeon ./themes/arcaeon` — this brings over `layouts/`, `assets/`, `static/fonts/`, `static/images/magus-hero.jpg`. The magus-hero.jpg is harmless baggage — the Rewired index layout (Plan 02) won't reference it, so it simply won't appear in the build output.
    3. Write `hugo.toml` at project root using the structure from `/Users/falkensmage/RitualSync/falkensmage-website/hugo.toml` but with these exact values:
       ```toml
       baseURL = "/"
       languageCode = "en-us"
       title = "Rewired"
       theme = "arcaeon"

       [params]
         author = "Matt Stine & Jaye Anne Beringer"
         description = "A podcast about what happens when people leave rigid belief systems and start finding something alive."
         showName = "Rewired"
         tagline = "We left the systems. We're finding what's alive."
         launchDate = "June 2026"

       [params.colors]
         # ARCÆON palette — Radiant Core subset primary for Rewired (D-11)
         voidPurple = "#1a0f2e"
         deepIndigo = "#0a0f3c"
         fusionGold = "#ffb347"
         ignitionOrange = "#ff7a18"
         plasmaPink = "#ff5fd2"
         ionGlow = "#5be7ff"
         solarWhite = "#fff4e6"
         electricViolet = "#7a2cff"

       [caches]
         [caches.getresource]
           dir = ":cacheDir/:project"
           maxAge = "1h"
       ```
       Note the intentional differences from falkensmage-website's hugo.toml: `baseURL = "/"` (domain is unknown until Phase 2 — use relative root so local builds work); `title = "Rewired"`; the `[params]` keys are Rewired-specific; `[params.colors]` is trimmed to the Radiant Core subset Rewired actually uses (Void Purple, Deep Indigo, Fusion Gold, Ignition Orange, Plasma Pink, Ion Glow, Solar White, Electric Violet only — no Neon Magenta, Electric Blue, Midnight Blue since Rewired's palette per D-11 is warm-core, not cool-core).
    4. Write `.gitignore` at project root with at least:
       ```
       # Hugo
       /public/
       /resources/_gen/
       .hugo_build.lock
       hugo_stats.json

       # macOS
       .DS_Store

       # Editor
       .idea/
       .vscode/
       ```
    5. Write a STUB `content/_index.md` with only frontmatter (Plan 02 fills in body copy):
       ```
       ---
       title: "Rewired"
       ---
       ```
    6. Verify the theme vendor: `ls themes/arcaeon/layouts/_default/baseof.html && ls themes/arcaeon/assets/css/main.css && ls themes/arcaeon/static/fonts/cinzel-latin-wght-normal.woff2` — all three must exist.
    7. Attempt a baseline Hugo build: `hugo --minify --destination /tmp/rewired-baseline-build 2>&1 | tee /tmp/hugo-baseline.log`. EXPECTED outcome: the build completes (exit 0) and produces `/tmp/rewired-baseline-build/index.html`. The rendered page will look like a broken/partial falkensmage homepage at this stage because the theme's `layouts/index.html` references Matt's personal partials (currently, social, identity-cta, footer) that will try to render from `_index.md` params that don't exist yet. That is EXPECTED — Plan 02 overrides `layouts/index.html` at the project level. What matters here is that Hugo resolves the theme and exits 0.

    Do NOT edit any file inside `themes/arcaeon/` — treat the vendored theme as read-only. All Rewired-specific customization goes in project-root `layouts/`, `assets/`, `content/`, `static/` which Hugo's layout lookup order will prefer over the theme's equivalents.
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/rewired-site &amp;&amp; test -f hugo.toml &amp;&amp; test -f .gitignore &amp;&amp; test -f themes/arcaeon/layouts/_default/baseof.html &amp;&amp; test -f themes/arcaeon/assets/css/main.css &amp;&amp; test -f themes/arcaeon/static/fonts/cinzel-latin-wght-normal.woff2 &amp;&amp; grep -q 'theme = "arcaeon"' hugo.toml &amp;&amp; grep -q 'voidPurple = "#1a0f2e"' hugo.toml &amp;&amp; grep -q 'fusionGold = "#ffb347"' hugo.toml &amp;&amp; hugo --minify --destination /tmp/rewired-baseline-build &amp;&amp; test -f /tmp/rewired-baseline-build/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `hugo.toml` exists at project root with `theme = "arcaeon"` and `[params.colors]` block containing at minimum `voidPurple`, `fusionGold`, `ignitionOrange`, `plasmaPink`, `ionGlow`, `solarWhite`
    - `themes/arcaeon/` is a full directory copy — `layouts/_default/baseof.html`, `layouts/index.html`, `assets/css/main.css`, `static/fonts/cinzel-latin-wght-normal.woff2`, `static/fonts/space-grotesk-latin-wght-normal.woff2` all exist
    - `.gitignore` excludes `/public/`, `/resources/_gen/`, `.hugo_build.lock`, `.DS_Store`
    - `content/_index.md` exists with `title: "Rewired"` frontmatter (body empty — Plan 02 fills it)
    - `hugo --minify --destination /tmp/rewired-baseline-build` exits 0 and produces `/tmp/rewired-baseline-build/index.html`
    - No files under `themes/arcaeon/` are modified (the theme is vendored read-only)
  </acceptance_criteria>
  <done>Hugo project scaffolded, arcaeon theme vendored at `themes/arcaeon/`, `hugo.toml` wired with palette params, baseline `hugo` build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Install host portraits as swap-ready static assets</name>
  <files>static/images/portraits/matt.jpg, static/images/portraits/jaye-anne.jpg</files>
  <read_first>
    - /Users/falkensmage/RitualSync/rewired-site/.planning/phases/01-build-static-site-content-portraits-palette/01-CONTEXT.md (D-02, D-03)
    - /Users/falkensmage/RitualSync/rewired-site/.planning/STATE.md (Assets & References block — source paths)
  </read_first>
  <action>
    Per D-02: copy the two portrait source files into `static/images/portraits/` with canonical filenames. Paths in Plan 02's Hero layout will reference `images/portraits/matt.jpg` and `images/portraits/jaye-anne.jpg` via Hugo's `relURL`. This establishes the swap-ready pattern: if Jaye Anne picks a different variant from https://mstine.github.io/rewired-portrait-review/, overwriting `static/images/portraits/jaye-anne.jpg` is a single-file replacement with zero code change (satisfies HERO-04).

    Steps:
    1. `mkdir -p /Users/falkensmage/RitualSync/rewired-site/static/images/portraits`
    2. `cp "$HOME/Documents/Business-Brand/Feral-Architecture/portraits/2026-04-13-author-headshot/final/author-headshot.jpg" /Users/falkensmage/RitualSync/rewired-site/static/images/portraits/matt.jpg`
    3. `cp "$HOME/Documents/Business-Brand/Feral-Architecture/portraits/2026-04-18-jaye-anne-headshot/final/jaye-anne-headshot.jpg" /Users/falkensmage/RitualSync/rewired-site/static/images/portraits/jaye-anne.jpg`
    4. Verify aspect ratio with ImageMagick's `identify` (or `sips -g pixelWidth -g pixelHeight` on macOS). Per D-03, both portraits must be 1:1 square to render at matched sizes. If either file is not 1:1:
       - For `matt.jpg`: the source is a locked Feral Architecture plate, which is already 1:1. If `identify` reports non-square, STOP and surface the discrepancy — do NOT auto-crop; the plate's composition is deliberate.
       - For `jaye-anne.jpg`: same — the Feral Architecture plate is 1:1 by design. If not, stop and surface.
    5. Record the two image dimensions (width × height, file size in bytes) in the task's completion output for the SUMMARY.
    6. Re-run `hugo --minify --destination /tmp/rewired-baseline-build` to confirm Hugo still builds and that the portrait files now appear at `/tmp/rewired-baseline-build/images/portraits/matt.jpg` and `/images/portraits/jaye-anne.jpg` (Hugo copies `static/` content into the build output verbatim).
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/rewired-site &amp;&amp; test -f static/images/portraits/matt.jpg &amp;&amp; test -f static/images/portraits/jaye-anne.jpg &amp;&amp; test $(stat -f%z static/images/portraits/matt.jpg 2>/dev/null || stat -c%s static/images/portraits/matt.jpg) -gt 10000 &amp;&amp; test $(stat -f%z static/images/portraits/jaye-anne.jpg 2>/dev/null || stat -c%s static/images/portraits/jaye-anne.jpg) -gt 10000 &amp;&amp; hugo --minify --destination /tmp/rewired-baseline-build &amp;&amp; test -f /tmp/rewired-baseline-build/images/portraits/matt.jpg &amp;&amp; test -f /tmp/rewired-baseline-build/images/portraits/jaye-anne.jpg</automated>
  </verify>
  <acceptance_criteria>
    - `static/images/portraits/matt.jpg` exists, is > 10 KB, and is a valid JPEG readable by `sips`/`identify`
    - `static/images/portraits/jaye-anne.jpg` exists, is > 10 KB, and is a valid JPEG
    - Both files have 1:1 aspect ratio (if not, task halts and surfaces the mismatch — no auto-crop)
    - A fresh `hugo` build produces both portraits at `/images/portraits/matt.jpg` and `/images/portraits/jaye-anne.jpg` in the output tree
    - Image dimensions and bytes recorded in task output for SUMMARY
  </acceptance_criteria>
  <done>Both portraits installed at canonical paths; `hugo` build includes them in output; swap-ready pattern established for HERO-04.</done>
</task>

<task type="auto">
  <name>Task 3: Establish typography decision and Rewired CSS override file</name>
  <files>assets/css/rewired-overrides.css, themes/arcaeon/layouts/_default/baseof.html (READ-ONLY — inspected, not modified), static/fonts/ (conditional — only if font swap is chosen)</files>
  <read_first>
    - /Users/falkensmage/RitualSync/rewired-site/.planning/phases/01-build-static-site-content-portraits-palette/01-CONTEXT.md (D-04, D-11, D-12)
    - /Users/falkensmage/RitualSync/rewired-site/themes/arcaeon/assets/css/main.css (entire file — understand the theme's typography block and semantic aliases)
    - /Users/falkensmage/RitualSync/rewired-site/themes/arcaeon/layouts/_default/baseof.html (understand how fonts preload)
  </read_first>
  <action>
    Per D-04: decide whether the theme's inherited Cinzel (display) + Space Grotesk (body) pairing satisfies "literary serif + clean geometric sans — adult, not cute or corporate." D-04 explicitly permits inheritance: "The arcaeon theme's existing typography is the starting point — if it already ships Fraunces/Inter or a close equivalent, inherit."

    **Recommended decision:** INHERIT Cinzel + Space Grotesk. Cinzel is a literary Roman-inscription serif (adult, not cute); Space Grotesk is a clean neo-grotesque (geometric sans). The pairing already reads as literary/adult on falkensmage.com which passed the same brand filter. Choosing Fraunces/Inter would add network weight and font-loading complexity for a marginal aesthetic difference.

    **If inheriting (recommended path):**
    1. Create `assets/css/rewired-overrides.css` containing ONLY Rewired-specific rules (no font swap). Minimum contents:
       ```css
       /* Rewired site overrides — layered on top of themes/arcaeon/assets/css/main.css */
       /* Per D-11: Rewired uses RitualSync warm-core subset of the ARCÆON palette on Void Purple base */

       :root {
         /* Rewired-specific semantic aliases — pin to warm-core per D-11 */
         --rewired-bg:          var(--arcaeon-void-purple);
         --rewired-bg-alt:      var(--arcaeon-deep-indigo);
         --rewired-text:        var(--arcaeon-solar-white);
         --rewired-heading:     var(--arcaeon-fusion-gold);   /* h1/h2 per D-05, D-11 */
         --rewired-cta:         var(--arcaeon-ignition-orange); /* D-11: CTA bg */
         --rewired-link-hover:  var(--arcaeon-plasma-pink);   /* D-11: link hover */
         --rewired-accent-cyan: var(--arcaeon-ion-glow);      /* D-11: ONE accent — reserved for Plan 02 focus ring */
         --rewired-divider:     var(--arcaeon-deep-indigo);

         /* Responsive breakpoints per D-12 — use as media query values, not as CSS vars directly */
       }

       /* D-12: mobile-first base styles, no horizontal scroll at any width */
       html, body { overflow-x: hidden; }

       /* D-12: touch targets >= 44x44px — applied to buttons and links in interactive regions */
       .rewired-cta, button.rewired-cta, a.rewired-cta {
         min-height: 44px;
         min-width: 44px;
       }

       /* Placeholder section rules — Plan 02 will extend these with Hero, Lens, Hosts, Coming Soon */
       .rewired-section {
         padding: clamp(2rem, 6vw, 6rem) clamp(1rem, 4vw, 3rem);
       }
       ```
    2. **Wire the override CSS into the base layout.** The theme's `baseof.html` uses `resources.Get "css/main.css" | css.Build | minify | fingerprint` which reads from EITHER `themes/arcaeon/assets/` OR project-root `assets/` (Hugo merges the two, project-root wins on collision). A separate Rewired file at `assets/css/rewired-overrides.css` is NOT auto-included by `main.css`. Two options — pick ONE and record the choice:

       **Option A (preferred — no theme modification):** Plan 02 will create `layouts/_default/baseof.html` at the PROJECT root that overrides the theme's baseof. That project-level baseof will add a second `<link rel="stylesheet">` pointing at the bundled `rewired-overrides.css`. For THIS plan, `rewired-overrides.css` just needs to exist at `assets/css/rewired-overrides.css` — Plan 02 handles the wiring.

       **Option B:** Use Hugo's `assets/css/main.css` shadowing — create `assets/css/main.css` at project root that first `@import`s the theme's version then the overrides. This is more fragile because the theme's `main.css` isn't served via a URL Hugo can import; it would require re-running the full pipeline. Do NOT pick this.

       Pick Option A. Leave wiring to Plan 02.
    3. Verify the override file parses as valid CSS: `hugo --minify --destination /tmp/rewired-baseline-build` still succeeds (Hugo's `css.Build` validates syntax when the file is referenced, but at this stage it's an orphan asset and won't be processed — that's fine).
    4. Record the typography decision in the task output: `TYPOGRAPHY_DECISION=inherit` (Cinzel + Space Grotesk retained, no override). If the executor genuinely judges the theme fonts insufficient and chooses the override path, they must instead:
       - `curl` Fraunces-variable and Inter-variable woff2 files into `static/fonts/`
       - Add `@font-face` declarations in `rewired-overrides.css` with matching paths
       - Add `body { font-family: 'Inter Variable', system-ui, sans-serif; }` and `h1, h2, h3 { font-family: 'Fraunces Variable', Georgia, serif; }` overrides
       - Record `TYPOGRAPHY_DECISION=override` and the font filenames in task output for SUMMARY

    **If overriding (fallback path — only if Cinzel/Space Grotesk genuinely fail the literary/adult test):** follow the override sub-steps above in step 4. Document the rationale explicitly in the task output.

    Do NOT modify any file under `themes/arcaeon/`. The theme stays read-only.
  </action>
  <verify>
    <automated>cd /Users/falkensmage/RitualSync/rewired-site &amp;&amp; test -f assets/css/rewired-overrides.css &amp;&amp; grep -q 'var(--arcaeon-void-purple)' assets/css/rewired-overrides.css &amp;&amp; grep -q 'var(--arcaeon-fusion-gold)' assets/css/rewired-overrides.css &amp;&amp; grep -q 'var(--arcaeon-ion-glow)' assets/css/rewired-overrides.css &amp;&amp; grep -q 'min-height: 44px' assets/css/rewired-overrides.css &amp;&amp; hugo --minify --destination /tmp/rewired-baseline-build &amp;&amp; test $(find themes/arcaeon -newer hugo.toml -type f 2>/dev/null | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `assets/css/rewired-overrides.css` exists and references at minimum `--arcaeon-void-purple`, `--arcaeon-fusion-gold`, `--arcaeon-ignition-orange`, `--arcaeon-plasma-pink`, `--arcaeon-ion-glow`, `--arcaeon-solar-white` via `var()`
    - The Rewired-specific semantic aliases (`--rewired-bg`, `--rewired-heading`, `--rewired-cta`, `--rewired-link-hover`, `--rewired-accent-cyan`) are defined
    - Touch-target rule `min-height: 44px` is present (D-12 compliance)
    - Typography decision is recorded in task output (`TYPOGRAPHY_DECISION=inherit` or `=override` with rationale)
    - No files under `themes/arcaeon/` were modified (find by mtime check, or manual git status shows theme dir untouched)
    - `hugo --minify` still exits 0
  </acceptance_criteria>
  <done>Rewired CSS overrides file exists with palette and responsive foundation; typography decision recorded; theme directory untouched; baseline build still succeeds. Plan 02 can now wire overrides into a project-level baseof.html and extend with section-specific styles.</done>
</task>

</tasks>

<verification>
1. **Hugo build** — `hugo --minify --destination /tmp/rewired-baseline-build` exits 0 and produces `/tmp/rewired-baseline-build/index.html` (content will look like a partial falkensmage layout at this stage — that is EXPECTED; Plan 02 replaces the homepage).
2. **Portraits served** — `/tmp/rewired-baseline-build/images/portraits/matt.jpg` and `/images/portraits/jaye-anne.jpg` exist in the output tree.
3. **Palette tokens resolve** — A manual `grep -r 'arcaeon-void-purple' themes/arcaeon/assets/css/main.css` confirms `--arcaeon-void-purple: #1a0f2e` is defined at `:root`. A grep against `assets/css/rewired-overrides.css` confirms warm-core tokens are referenced via `var()`.
4. **Theme read-only** — `git status themes/arcaeon/` shows the tree as added but with no subsequent modifications after the initial copy. No theme files were edited.
5. **Swap-ready pattern** — Dry-run: if you `cp /tmp/alt-jaye-anne.jpg static/images/portraits/jaye-anne.jpg` and re-run `hugo`, the new image appears in the build output with no other changes required. (Executor confirms logically; no need to actually swap.)
</verification>

<success_criteria>
- Hugo project scaffolded with `hugo.toml`, `.gitignore`, `content/_index.md` at project root
- Arcaeon theme vendored at `themes/arcaeon/` (full tree copied from falkensmage-website)
- Both host portraits at `static/images/portraits/matt.jpg` and `static/images/portraits/jaye-anne.jpg` — 1:1 square, > 10 KB each
- `assets/css/rewired-overrides.css` exists with warm-core palette aliases, responsive touch-target rule, and D-12 no-horizontal-scroll guard
- Typography decision (inherit vs override) is recorded in SUMMARY with rationale
- Baseline `hugo --minify` build succeeds
- No files inside `themes/arcaeon/` were modified after the initial copy
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-static-site-content-portraits-palette/01-01-SUMMARY.md` including:
- Baseline Hugo build success + output directory confirmation
- Portrait dimensions and byte sizes for both files
- Typography decision: `TYPOGRAPHY_DECISION=inherit` (or `=override` with rationale + filenames)
- Any deviations from D-01 (theme vendoring via copy instead of submodule — explain the source repo constraint)
- Exact list of files created so Plan 02 has a manifest to build on
</output>
